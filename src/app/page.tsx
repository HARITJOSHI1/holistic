import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return (
    <div>
      <Button variant="ghost">Press me</Button>
    </div>
  );
};

export default page;
